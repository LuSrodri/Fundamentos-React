import React from "react"


export default function First() {
    let msg = "Seja Bem-vindo (a) !!!!!"
    return (
        <div>
            <h2>Primeiro components</h2>
            <p>{ msg }</p>
        </div>
    )
}